services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - php
      - phpmyadmin
    expose:
      - "80"

  php:
    build:
      context: .
      dockerfile: Dockerfile.php
    restart: unless-stopped
    depends_on:
      - db
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: ${MYSQL_DATABASE}
      DB_USERNAME: ${MYSQL_USER}
      DB_PASSWORD: ${MYSQL_PASSWORD}
    # Servico interno (PHP-FPM), sem dominio publico no Coolify.
    expose:
      - "9000"

  db:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin:5-apache
    restart: unless-stopped
    depends_on:
      - db
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      # Use a URL completa com /db/ no Coolify.
      PMA_ABSOLUTE_URI: ${PMA_ABSOLUTE_URI:-https://basedoprumo.chatboot.cloud/db/}
    expose:
      - "80"

volumes:
  mysql_data:
